services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telepharmacy-api
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      PORT: "3000"
      SEED_FILE_PATH: /app/db.json
    restart: unless-stopped
